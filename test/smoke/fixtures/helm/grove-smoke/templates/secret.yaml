apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-jwt
  labels:
    {{- include "grove-smoke.labels" . | nindent 4 }}
type: Opaque
data:
  jwt-secret: {{ .Values.jwt.secret | b64enc | quote }}
