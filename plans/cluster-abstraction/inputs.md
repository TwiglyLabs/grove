
## From plans
- **clean-library-api** — clean API surface with structured return values (no stdout side-effects), consistent typed results across environment slice functions

## From existing code
- `src/environment/cluster.ts` — existing cluster code (~30 lines, two functions) to be wrapped behind the abstraction layer
