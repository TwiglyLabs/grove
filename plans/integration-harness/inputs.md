
## From plans
- **environment-descriptor** — `describe()` function for post-bring-up verification of environment state
- **enhanced-pruning** — `prune()` for cleanup-phase validation and confirming resources are removed after teardown
- **setup-automation** — automatic setup execution after `workspace.create()` so the harness can boot a full environment without manual steps
